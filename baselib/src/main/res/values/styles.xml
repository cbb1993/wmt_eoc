<resources>

    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- 设置activity切换动画 -->
<!--        <item name="android:windowFullscreen">true</item>-->
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:buttonStyle">@style/Widget.AppCompat.Button.Borderless</item>
        <item name="android:textColor">@color/lib_normal</item>
        <item name="android:windowBackground">@color/bg_f5</item>
        <item name="colorAccent">@color/lib_blue_4c98ea</item>
        <item name="android:fitsSystemWindows">false</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">@color/lib_transparent</item>
<!--        <item name="android:windowTranslucentNavigation">true</item>-->
    </style>

    <!-- 浮动窗口动画 -->
    <style name="anim_float_view">
        <item name="android:windowEnterAnimation">@anim/push_up_in</item>
        <item name="android:windowExitAnimation">@anim/push_up_out</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Holo.Light">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="anim_image_page">
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit</item>
    </style>
</resources>
